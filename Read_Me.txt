launch application by spinning up mySQL (rganye, login database) and running the formMediator.js
formMediator exposes the login and signup endpoints that get called by the index.html page.
Form mediator also exposes the index page endpoint that redirects a user to the index page.

The indexpage presents the user with a sign in or sign up tab. credentials are stored in a
sequential database, mySQL.